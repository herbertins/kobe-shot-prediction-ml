kobe_dev_data:
  type: pandas.ParquetDataset
  filepath: data/raw/dataset_kobe_dev.parquet

kobe_prod_data:
  type: pandas.ParquetDataset
  filepath: data/raw/dataset_kobe_prod.parquet

kobe_filtered_data:
  type: pandas.ParquetDataset
  filepath: data/intermediate/data_filtered.parquet
  save_args:
    index: False

kobe_train_data:
  type: pandas.ParquetDataset
  filepath: data/primary/base_train.parquet
  save_args:
    index: False

kobe_test_data:
  type: pandas.ParquetDataset
  filepath: data/primary/base_test.parquet
  save_args:
    index: False

kobe_predictions:
  type: pandas.ParquetDataset
  filepath: data/model_output/predictions_prod.parquet
  save_args:
    index: False

